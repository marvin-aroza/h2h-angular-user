import { DateConvertorPipe } from './date-convertor.pipe';

describe('DateConvertorPipe', () => {
  it('create an instance', () => {
    const pipe = new DateConvertorPipe();
    expect(pipe).toBeTruthy();
  });
});
